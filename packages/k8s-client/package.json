{
  "name": "@cloudoperators/juno-k8s-client",
  "version": "1.0.0",
  "author": "UI-Team",
  "description": "JavaScript client for Kubernetes API",
  "main": "build/k8s-client.es.js",
  "module": "build/k8s-client.es.js",
  "types": "build/types/*.d.ts",
  "files": [
    "build/**",
    "LICENCE"
  ],
  "scripts": {
    "test": "vitest run",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "clean": "rm -rf build && rm -rf node_modules && rm -rf .turbo"
  },
  "keywords": [
    "kubernetes",
    "js",
    "typescript",
    "client"
  ],
  "devDependencies": {
    "@cloudoperators/juno-eslint-config": "*",
    "@cloudoperators/juno-typescript-config": "*",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "axios": "^1.7.3"
  }
}
