---
"@cloudoperators/juno-ui-components": patch
---

Fix issues in `SideNavigation`,`SideNavigationItem` and `SideNavigationGroup`:
- Removed `disabled` prop from SideNavigation
- Removed `activeItem` and `onActiveItemChange` prop from SideNavigation
- Removed `disabled` prop from SidenavigationGroup
- Fixed multiple issues in all components