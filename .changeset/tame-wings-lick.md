---
"@cloudoperators/juno-app-doop": patch
---

After switching to the new URL structure, resolved filter types in Doop were not being reflected in the URL, causing filtering issues. This update makes sure that the URL updates once filter data is resolved.
