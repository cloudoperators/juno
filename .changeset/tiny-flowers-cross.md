---
"@cloudoperators/juno-ui-components": patch
---

fix(ui): Badge - inline-flex is now always applied by default. Previously this was only done in case of using Badge with icon. This change leads to consistent rendering with and without icon.
