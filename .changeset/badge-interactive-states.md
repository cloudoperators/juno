---
"@cloudoperators/juno-ui-components": minor
---

feat(Badge): add interactive states. Pass `onClick` to render the badge as a `<button>`, or `href` to render it as an `<a>` element. Both support hover, `:active`, focus, and disabled states. Use `disabled` to disable the element.
