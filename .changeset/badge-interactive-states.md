---
"@cloudoperators/juno-ui-components": minor
---

feat(Badge): add interactive states. Set `interactive` to render the badge as a `<button>` with hover, `:active`, focus, and disabled states. Use `disabled` to disable the button.
